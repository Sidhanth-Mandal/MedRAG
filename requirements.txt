# ============================================================
#  requirements.txt — Agentic RAG Medical Q&A
# ============================================================

# ── Core ────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Data Ingestion ───────────────────────────────────────────
biopython>=1.83          # Entrez / PubMed API
requests>=2.31.0
beautifulsoup4>=4.12.0   # HTML parsing fallback
lxml>=5.1.0

# ── Chunking ────────────────────────────────────────────────
tiktoken>=0.7.0          # Token counting for chunk sizing

# ── Vector Store ────────────────────────────────────────────
qdrant-client>=1.9.0

# ── Sparse Retrieval ────────────────────────────────────────
rank-bm25>=0.2.2

# ── Reranking ───────────────────────────────────────────────
cohere>=5.5.0

# ── LLM / Agent ─────────────────────────────────────────────
groq>=0.9.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.6
langchain-cohere>=0.1.9
langgraph>=0.1.19
langgraph-checkpoint-postgres>=1.0.0

# ── Supabase / Postgres ──────────────────────────────────────
supabase>=2.5.0
psycopg2-binary>=2.9.9
asyncpg>=0.29.0

# ── API ─────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# ── Utilities ───────────────────────────────────────────────
tqdm>=4.66.0
tenacity>=8.3.0          # Retry logic
httpx>=0.27.0

# ── Authentication ───────────────────────────────────────────
PyJWT>=2.8.0                       # JWT signing / verification (replaces python-jose)
passlib[bcrypt]>=1.7.4             # Password hashing
